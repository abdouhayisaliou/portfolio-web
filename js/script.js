const phrases=["Sites vitrines modernes","WordPress professionnel","HTML • CSS • JavaScript","Sites rapides et responsives","Design orienté conversion"];let phraseIndex=0,charIndex=0,deleting=false;const typing=document.getElementById("typing");function typeEffect(){if(!typing)return;const current=phrases[phraseIndex];typing.textContent=current.slice(0,charIndex);if(!deleting&&charIndex<current.length){charIndex++;setTimeout(typeEffect,58)}else if(deleting&&charIndex>0){charIndex--;setTimeout(typeEffect,28)}else{deleting=!deleting;if(!deleting)phraseIndex=(phraseIndex+1)%phrases.length;setTimeout(typeEffect,deleting?1200:300)}}typeEffect();const menu=document.querySelector(".menu-toggle");const nav=document.querySelector(".navbar");if(menu&&nav)menu.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.setAttribute("aria-expanded",open?"true":"false")});const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")})},{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));const filterButtons=document.querySelectorAll(".filter-btn");const cards=document.querySelectorAll(".work-card[data-category], .home-project[data-category]");filterButtons.forEach(button=>{button.addEventListener("click",()=>{filterButtons.forEach(btn=>btn.classList.remove("active"));button.classList.add("active");const filter=button.dataset.filter;cards.forEach(card=>{const show=filter==="all"||card.dataset.category===filter;card.style.display=show?"":"none"})})});const form=document.getElementById("contactForm");if(form){form.addEventListener("submit",event=>{event.preventDefault();const data=new FormData(form);const subject=encodeURIComponent("Demande de devis - "+data.get("projectType"));const body=encodeURIComponent("Bonjour Abdou-Hayi,\n\nJe souhaite discuter d'un projet web.\n\nType de projet : "+data.get("projectType")+"\nBudget estimatif : "+data.get("budget")+"\n\nMessage :\n"+data.get("message")+"\n\nNom : "+data.get("name")+"\nEmail : "+data.get("email"));window.location.href="mailto:abdouhayisaliou@gmail.com?subject="+subject+"&body="+body})}